type Post {
    id: ID
    userId: ID
    userName: String
    questionId: ID
    questionTitle: String
    difficulty: String
    solvedAt: String
    likesCount: Int
    commentsCount: Int
    createdAt: String
    visibility: PostVisibility
    primaryImageUrl: String
}

enum PostVisibility {
    PUBLIC
    FRIENDS
    PRIVATE
}